---
type: knowledge
tags: [python, 函数式编程, 基础层]
title: Python装饰器
author: 云原生技术架构师
---

# Python装饰器 (Decorators)

## 💡 核心概念
装饰器本质上是一个Python函数，它可以让其他函数在不需要做任何代码变动的前提下增加额外功能。装饰器的返回值通常也是一个函数对象。
- **设计模式**：装饰器模式
- **语法糖**：`@decorator` 等价于 `func = decorator(func)`

## 🔍 主要应用场景
1. **切面编程 (AOP)**：日志记录、性能测试、事务处理、缓存、权限校验。
2. **代码复用**：提取公共逻辑，减少重复代码。

## 💻 代码示例

### 1. 基础函数装饰器
```python
import functools
import time

def timer(func):
    """打印函数执行时间的装饰器"""
    @functools.wraps(func)  # 保留原函数的元数据（如docstring, __name__）
    def wrapper(*args, **kwargs):
        start = time.time()
        result = func(*args, **kwargs)
        end = time.time()
        print(f"{func.__name__} executed in {end - start:.4f}s")
        return result
    return wrapper

@timer
def heavy_computation():
    time.sleep(1)

heavy_computation()
```

### 2. 带参数的装饰器
```python
def repeat(times):
    def decorator(func):
        @functools.wraps(func)
        def wrapper(*args, **kwargs):
            for _ in range(times):
                result = func(*args, **kwargs)
            return result
        return wrapper
    return decorator

@repeat(times=3)
def greet(name):
    print(f"Hello, {name}")
```

### 3. 类装饰器
```python
def singleton(cls):
    instances = {}
    def get_instance(*args, **kwargs):
        if cls not in instances:
            instances[cls] = cls(*args, **kwargs)
        return instances[cls]
    return get_instance

@singleton
class Database:
    pass
```

## ⚠️ 最佳实践
- 始终使用 `@functools.wraps(func)`，否则原函数的 `__name__` 和 `__doc__` 会丢失。
- 装饰器叠加时的执行顺序：**从下往上**装饰，**从上往下**执行（类似于洋葱模型）。

## 🔗 相关知识
- [[基础层/Python/元类|元类]]：在类定义级别修改行为。
- [[基础层/Python/上下文管理器|上下文管理器]]：处理资源管理的另一种方式。
