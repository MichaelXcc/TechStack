---
type: concept
tags: [架构层, AIInfra, Volcano, 网络拓扑, 调度优化]
title: 网络拓扑感知调度原理
created: 2026-01-21
updated: 2026-01-21
author: AI Agent
---

# 网络拓扑感知调度原理

## 1. 为什么需要网络拓扑感知？

### 1.1 分布式训练的通信挑战

```mermaid
graph TB
    subgraph "大模型训练"
        M[模型参数]
        W1[Worker 1]
        W2[Worker 2]
        W3[Worker 3]
        W4[Worker 4]
    end
    
    W1 <-->|梯度同步| W2
    W2 <-->|梯度同步| W3
    W3 <-->|梯度同步| W4
    W4 <-->|梯度同步| W1
```

**通信瓶颈**：
- 大模型训练涉及频繁的梯度同步 (All-Reduce)
- 每次迭代可能需要传输 GB 级数据
- 网络性能直接影响训练效率

### 1.2 网络拓扑的复杂性

```mermaid
graph TB
    subgraph "数据中心网络拓扑"
        Core[核心交换机<br/>Spine]
        
        subgraph "机架 1"
            ToR1[ToR 交换机]
            N1[Node 1]
            N2[Node 2]
        end
        
        subgraph "机架 2"
            ToR2[ToR 交换机]
            N3[Node 3]
            N4[Node 4]
        end
        
        subgraph "机架 3 - NVSwitch Domain"
            ToR3[ToR 交换机]
            NV[NVSwitch]
            N5[Node 5]
            N6[Node 6]
        end
    end
    
    Core --- ToR1
    Core --- ToR2
    Core --- ToR3
    
    ToR1 --- N1
    ToR1 --- N2
    ToR2 --- N3
    ToR2 --- N4
    ToR3 --- N5
    ToR3 --- N6
    NV === N5
    NV === N6
```

**拓扑层级与带宽**：

| 层级 | 典型带宽 | 延迟 |
|------|---------|------|
| NVLink (同节点 GPU) | 600 GB/s | ~1μs |
| NVSwitch (跨节点 GPU) | 400 GB/s | ~2μs |
| InfiniBand HDR | 200 Gb/s | ~1μs |
| RoCE v2 | 100 Gb/s | ~2μs |
| 同机架以太网 | 25 Gb/s | ~10μs |
| 跨机架以太网 | 25 Gb/s | ~50μs |

> [!IMPORTANT]
> 同一 NVSwitch 域内的通信速度可能是跨机架通信的 **10-100 倍**！智能调度可以显著提升训练效率。

### 1.3 传统调度的问题

```mermaid
graph LR
    subgraph "传统调度 (拓扑无感)"
        J1[4 GPU Job]
        N1[Node 1<br/>Rack 1]
        N2[Node 3<br/>Rack 2]
        N3[Node 5<br/>Rack 3]
    end
    
    J1 --> N1
    J1 --> N2
    J1 --> N3
    
    style N1 fill:#f99
    style N2 fill:#9f9
    style N3 fill:#99f
```

**问题**：Pod 分散到不同机架，跨机架通信成为瓶颈。

## 2. HyperNode 抽象

### 2.1 什么是 HyperNode？

HyperNode 是 Volcano 定义的 CRD，用于描述网络拓扑中的**性能域**：

```yaml
apiVersion: topology.volcano.sh/v1alpha1
kind: HyperNode
metadata:
  name: nvswitch-domain-1
spec:
  tier: 1              # 拓扑层级
  members:
    - type: Node
      selector:
        nvidia.com/nvswitch-domain: "domain-1"
```

### 2.2 拓扑层级模型

```mermaid
graph TB
    subgraph "HyperNode 层级"
        H0[Tier 0: GPU<br/>NVLink 域内]
        H1[Tier 1: NVSwitch<br/>跨节点 GPU]
        H2[Tier 2: InfiniBand<br/>同子网]
        H3[Tier 3: Ethernet<br/>跨机架]
    end
    
    H0 --> H1
    H1 --> H2
    H2 --> H3
    
    style H0 fill:#4CAF50
    style H1 fill:#8BC34A
    style H2 fill:#CDDC39
    style H3 fill:#FFEB3B
```

**层级设计原则**：
- **Tier 0**: 最高性能域 (NVLink)
- **Tier N+1**: 比 Tier N 性能更低的域
- 数字越小，性能越高

### 2.3 HyperNode 树形结构

```mermaid
graph TB
    Root[Root HyperNode<br/>Tier 3: 全集群]
    
    R1[Rack-1<br/>Tier 2]
    R2[Rack-2<br/>Tier 2]
    R3[Rack-3<br/>Tier 2]
    
    NV1[NVSwitch-1<br/>Tier 1]
    NV2[NVSwitch-2<br/>Tier 1]
    
    N1[Node-1<br/>Tier 0]
    N2[Node-2<br/>Tier 0]
    N3[Node-3<br/>Tier 0]
    N4[Node-4<br/>Tier 0]
    N5[Node-5<br/>Tier 0]
    N6[Node-6<br/>Tier 0]
    
    Root --> R1
    Root --> R2
    Root --> R3
    
    R1 --> N1
    R1 --> N2
    R2 --> N3
    R2 --> N4
    R3 --> NV1
    R3 --> NV2
    
    NV1 --> N5
    NV1 --> N6
```

## 3. 调度决策流程

### 3.1 拓扑感知调度流程

```mermaid
sequenceDiagram
    participant User
    participant Scheduler
    participant TopologyPlugin
    participant Cache
    
    User->>Scheduler: 提交 Job (networkTopology: hard)
    Scheduler->>Cache: 获取 HyperNode 信息
    Cache-->>Scheduler: 返回拓扑树
    
    Scheduler->>TopologyPlugin: 执行 Filter
    TopologyPlugin->>TopologyPlugin: 根据资源需求过滤 HyperNode
    TopologyPlugin-->>Scheduler: 候选 HyperNode 列表
    
    Scheduler->>TopologyPlugin: 执行 Score
    TopologyPlugin->>TopologyPlugin: 按 Tier 排序<br/>低 Tier 优先
    TopologyPlugin-->>Scheduler: 评分结果
    
    Scheduler->>Scheduler: 选择最优 HyperNode
    Scheduler->>User: 调度到同一性能域
```

### 3.2 打分策略

```go
// 简化的打分逻辑
func scoreHyperNode(hyperNode *HyperNode, job *Job) int {
    baseScore := 0
    
    // 1. Tier 越低分数越高
    baseScore += (maxTier - hyperNode.Tier) * 10
    
    // 2. 已调度的 Pod 越多分数越高 (亲和性)
    scheduledPods := countScheduledPods(hyperNode, job)
    baseScore += scheduledPods * 5
    
    // 3. 资源利用率适中的分数更高
    utilizationScore := calculateUtilization(hyperNode)
    baseScore += utilizationScore
    
    return baseScore
}
```

### 3.3 约束类型

| 约束类型 | 行为 | 适用场景 |
|---------|------|---------|
| **hard** | 必须调度到同一 HyperNode | 对通信延迟敏感 |
| **soft** | 尽量调度到同一 HyperNode | 对通信不太敏感 |

## 4. Job 配置示例

### 4.1 强约束 (Hard)

```yaml
apiVersion: batch.volcano.sh/v1alpha1
kind: Job
metadata:
  name: llm-training
spec:
  minAvailable: 8
  schedulerName: volcano
  queue: training
  networkTopology:
    mode: hard              # 强约束
    highestTierAllowed: 1   # 最多允许 Tier 1 (NVSwitch 域)
  tasks:
  - name: worker
    replicas: 8
    template:
      spec:
        containers:
        - name: trainer
          image: training:v1
          resources:
            limits:
              nvidia.com/gpu: 8
```

### 4.2 软约束 (Soft)

```yaml
apiVersion: batch.volcano.sh/v1alpha1
kind: Job
metadata:
  name: inference-job
spec:
  minAvailable: 4
  schedulerName: volcano
  networkTopology:
    mode: soft              # 软约束
    highestTierAllowed: 2   # 可以跨 Tier 2 (InfiniBand)
  tasks:
  - name: worker
    replicas: 4
    template:
      spec:
        containers:
        - name: inference
          image: inference:v1
          resources:
            limits:
              nvidia.com/gpu: 1
```

## 5. 分区调度 (SubJob)

### 5.1 大规模任务分区

对于大规模训练任务，可以使用分区策略：

```mermaid
graph TB
    subgraph "Job"
        S1[SubJob 1<br/>DP Group 1]
        S2[SubJob 2<br/>DP Group 2]
        S3[SubJob 3<br/>DP Group 3]
        S4[SubJob 4<br/>DP Group 4]
    end
    
    subgraph "HyperNodes"
        H1[NVSwitch Domain 1]
        H2[NVSwitch Domain 2]
        H3[NVSwitch Domain 3]
        H4[NVSwitch Domain 4]
    end
    
    S1 --> H1
    S2 --> H2
    S3 --> H3
    S4 --> H4
```

### 5.2 分区配置

```yaml
apiVersion: batch.volcano.sh/v1alpha1
kind: Job
metadata:
  name: large-scale-training
spec:
  minAvailable: 32
  schedulerName: volcano
  networkTopology:
    mode: hard
    highestTierAllowed: 1
  partitionPolicy:
    - name: dp-group-1
      replicas: 8
      networkTopology:
        mode: hard
        highestTierAllowed: 0    # 单节点内
    - name: dp-group-2
      replicas: 8
      networkTopology:
        mode: hard
        highestTierAllowed: 0
    - name: dp-group-3
      replicas: 8
      networkTopology:
        mode: hard
        highestTierAllowed: 0
    - name: dp-group-4
      replicas: 8
      networkTopology:
        mode: hard
        highestTierAllowed: 0
  tasks:
  - name: worker
    replicas: 32
    template:
      spec:
        containers:
        - name: trainer
          image: training:v1
          resources:
            limits:
              nvidia.com/gpu: 1
```

## 6. 调度优化效果

### 6.1 性能对比

```mermaid
xychart-beta
    title "分布式训练吞吐量对比"
    x-axis ["无拓扑感知", "Tier 2 (IB)", "Tier 1 (NVSwitch)", "Tier 0 (单节点)"]
    y-axis "相对吞吐量 (%)" 0 --> 100
    bar [45, 70, 90, 100]
```

### 6.2 典型优化效果

| 场景 | 无拓扑感知 | 拓扑感知 | 提升 |
|------|-----------|---------|------|
| 8 卡 LLM 训练 | 45% 效率 | 95% 效率 | 2.1x |
| 32 卡分布式训练 | 35% 效率 | 85% 效率 | 2.4x |
| All-Reduce 延迟 | 50ms | 5ms | 10x |

## 7. 两级调度策略

### 7.1 两级 Gang Scheduling

```mermaid
sequenceDiagram
    participant Scheduler
    participant Job
    participant SubJob1
    participant SubJob2
    
    Scheduler->>Job: 检查整体资源需求
    
    alt Job 级资源充足
        Scheduler->>SubJob1: 调度 SubJob 1
        Scheduler->>SubJob2: 调度 SubJob 2
        
        alt 所有 SubJob 成功
            Job-->>Scheduler: Job 调度成功
        else 部分 SubJob 失败
            Scheduler->>SubJob1: 回滚
            Scheduler->>SubJob2: 回滚
            Job-->>Scheduler: 等待资源
        end
    else Job 级资源不足
        Job-->>Scheduler: 等待资源
    end
```

### 7.2 配置两级调度

```yaml
spec:
  minAvailable: 32
  gangScheduling:
    enabled: true
    level: two        # 两级调度
  partitionPolicy:
    - name: partition-1
      minMember: 8    # 每个分区最少 8 个
```

## 8. 总结

| 概念 | 说明 |
|------|------|
| **HyperNode** | 网络拓扑中的性能域抽象 |
| **Tier** | 拓扑层级，数字越小性能越高 |
| **hard 约束** | 必须调度到同一 HyperNode |
| **soft 约束** | 尽量调度到同一 HyperNode |
| **分区调度** | 大规模任务分割为多个子任务 |
| **两级 Gang** | Job 和 SubJob 两级原子调度 |

> [!TIP]
> 网络拓扑感知调度是大模型训练的关键优化点。通过 HyperNode 抽象，Volcano 可以将分布式任务调度到最优的性能域，显著提升训练效率。
